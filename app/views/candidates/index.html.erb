



<%= link_to 'Add Candidate' , new_candidate_path %>


<table>
<tr>
    
    <td>Votes</td>
    <td>Count</td>
    <td>Name </td>
    <td>Party </td>
    <td>Age </td>
    <td>Policy </td>
    <td>Action </td>

</tr>



<% @candidates.each do |candidate| %>

<tr>
    <td><%= link_to 'Vote' , vote_candidate_path(candidate.id), method: 'Post' %> </td>
    <td><%= candidate.vote_logs.size%> </td>
    <td><%= link_to candidate.name, candidate_path(candidate.id) %>  </td>
    <td><%=  candidate.party%> </td>
    <td><%=  candidate.age%> </td>
    <td><%=  candidate.policy%> </td>
    <td>
    <%= link_to 'update', edit_candidate_path(candidate.id) %> 

    <%= link_to 'delete', candidate_path(candidate.id), method: 'delete', data: {confirm: '你確定？'}  %>
    </td>

</tr>
<% end %>

</table>


      